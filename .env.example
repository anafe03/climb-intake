# Copy to .env. Without an API key the service runs in rules-only mode.
ANTHROPIC_API_KEY=
# auto | llm | rules   (auto = llm when a key is present, else rules)
CLASSIFIER_MODE=auto
CLAUDE_MODEL=claude-opus-5
AUDIT_DB_PATH=./data/audit.db
